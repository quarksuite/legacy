import { Color } from './color';
declare const _default: {
    Color: typeof Color;
    color: (color: string) => Color;
    colour: (color: string) => Color;
    c: (color: string) => Color;
};
export default _default;
