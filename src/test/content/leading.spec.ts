describe('Content config', () => {
  describe('leading', () => {
    test('with defaults', () => {
      const data = { base: '1em', ratio: 1.333 };
      const schema = {
        base: { value: expect.any(Number) },
        narrow: { value: expect.any(Number) }
      };
    });
  });
});
