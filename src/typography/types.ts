/**
 * Some types for adding context to the typography functions
 *
 * @typeparam Fonts - all the valid keys for generating OS stacks
 * @typeparam Typography - The output OS stacks
 */
export type Fonts = ('sans' | 'serif' | 'monospace')[];
export type Typography = string | string[];
